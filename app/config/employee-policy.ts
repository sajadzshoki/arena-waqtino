/**
 * سیاست حذف پرسنل — یک جای قابل‌تنظیم، نه شرطِ پخش‌شده در کامپوننت‌ها.
 *
 * در حالت mock این اعداد قانون ساده‌ای را می‌سازند؛ در حالت api سرور نتیجه را در
 * خود پاسخ می‌دهد (`EmployeeRemovePolicy`) و UI هیچ‌چیز فرض نمی‌کند — پس اگر فردا
 * «حذف فقط با تأیید پشتیبانی» یا «حذف ممنوع وقتی دسترس‌پذیری ثبت شده» شد، همین
 * فایل و یک متد سرویس عوض می‌شود، نه چند صفحه.
 */
export const EMPLOYEE_REMOVAL_POLICY = {
  /** نوبت زنده (در انتظار/تأییدشدهٔ پیش‌رو) ⇒ حذف ممنوع؛ پیشنهاد: غیرفعال‌کردن */
  blockWhenLiveBookings: true,
  /** فقط تاریخچه ⇒ حذف مجاز (تاریخچه با اسنپ‌شات رزرو می‌ماند) */
  allowWhenHistoryOnly: true,
  /**
   * حذف ممنوع وقتی پرسنل به حساب کاربری متصل است؟ عمداً `false`: اتصال حساب یک
   * دلیل برای نگه‌داشتن *رکورد پرسنل* نیست — حساب کاربری موجودیت جداست و حذف
   * این رابطه، آن را حذف نمی‌کند. (فازهای اتصال حساب ممکن است این را عوض کنند.)
   */
  blockWhenAccountLinked: false,
  /** حداکثر «سن» پرسنل (روز) که در آن حذف آزاد است؛ null = بدون محدودیت */
  minAgeDaysBeforeRemove: null
} as const
