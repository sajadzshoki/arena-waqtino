/**
 * سیاست حذف سرویس — یک جای قابل‌تنظیم، نه شرطِ پخش‌شده در کامپوننت‌ها.
 *
 * در حالت mock این اعداد قانون ساده‌ای را می‌سازند؛ در حالت api سرور نتیجه را
 * در خود پاسخ می‌دهد (`ServiceDeletePolicy`) و UI هیچ‌چیز فرض نمی‌کند — پس اگر
 * فردا «حذف فقط با تأیید پشتیبانی» شد، همین فایل و یک متد سرویس عوض می‌شود.
 */
export const SERVICE_DELETION_POLICY = {
  /** نوبت زنده (در انتظار/تأییدشدهٔ پیش‌رو) ⇒ حذف ممنوع؛ پیشنهاد: غیرفعال‌کردن */
  blockWhenLiveBookings: true,
  /** فقط تاریخچه ⇒ حذف مجاز (تاریخچه با اسنپ‌شات رزرو می‌ماند) */
  allowWhenHistoryOnly: true,
  /** حداکثر «سن» سرویس (روز) که در آن حذف آزاد است؛ null = بدون محدودیت */
  minAgeDaysBeforeDelete: null
} as const
