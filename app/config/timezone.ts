/**
 * وقت مرجع اپ — تک‌نقطه‌ای که «امروز» و «ساعت چند است» در آن تعریف می‌شود.
 *
 * چرا یک فایل جدا؟ چون دسترس‌پذیری (فاز ۱۱) تنها جایی است که «کدام روز از هفته
 * است» و «این ساعت گذشته یا نه» معنای *مکان* دارد. اگر همین دو سوال در کامپوننت‌ها
 * با `new Date()` محلی پاسخ داده شوند، رزروِ یک مشتری در تورنتو برای جمعه‌ای
 * وقتینو می‌سازد که در تهران پنجشنبه است. پس:
 *   • همهٔ محاسبات روز/ساعت از `app/utils/schedule-time.ts` می‌گذرد،
 *   • آن فایل هم این ثابت را می‌خواند، نه `Intl` خام را در جای‌جای اپ.
 *
 * در حالت واقعی این مقدار از بک‌اند می‌آید (`business.timezone`)؛ تا آن موقع همین
 * یک خط عوض می‌شود — نه هفت کامپوننت.
 */
export const APP_TIMEZONE = 'Asia/Tehran'

/** دقیقه‌های گامِ پیشنهادیِ اسلات وقتی مدت سرویس تعریف‌نشده است. */
export const DEFAULT_SLOT_STEP_MINUTES = 30

/** چند روز آینده برای نوار تاریخ رزرو پرس‌وجو می‌شود (یک درخواست دسته‌ای). */
export const AVAILABILITY_HORIZON_DAYS = 14
